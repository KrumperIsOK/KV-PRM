target-version = "py311"

[lint]
select = ["E4", "E7", "E9", "F"]
ignore = [
  "E402", # Entry-point scripts add their sibling module directory to sys.path.
  "E731", # Dataset callbacks are intentionally returned as compact lambdas.
]
