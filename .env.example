# Optional Hugging Face cache location
HF_HOME=/path/to/huggingface-cache

# Only needed when using an OpenAI-compatible inference endpoint
OPENAI_API_KEY=replace-me
OPENAI_BASE_URL=http://127.0.0.1:8000/v1
